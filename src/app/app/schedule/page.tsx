import { EmptyState } from "@/components/shell/empty-state";

export default function ScheduleStub() {
  return (
    <EmptyState
      title="Schedule"
      description="Weekly grid, recurring templates, and authorization-first placement land in Phase 2."
    />
  );
}
