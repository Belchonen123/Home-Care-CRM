import { EmptyState } from "@/components/shell/empty-state";

export default function SettingsStub() {
  return (
    <EmptyState
      title="Settings"
      description="Agency profile, branding, EVV thresholds, integrations, and team management land across Phases 1–7."
    />
  );
}
