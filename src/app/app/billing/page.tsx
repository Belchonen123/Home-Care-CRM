import { EmptyState } from "@/components/shell/empty-state";

export default function BillingStub() {
  return (
    <EmptyState
      title="Billing"
      description="Invoice generation, 837P export, 835 reconciliation, and aging reports land in Phase 5."
    />
  );
}
