import { EmptyState } from "@/components/shell/empty-state";

export default function ReportsStub() {
  return (
    <EmptyState
      title="Reports"
      description="Authorization utilization, missed visits, payroll exports, and state-specific templates land in Phase 6."
    />
  );
}
