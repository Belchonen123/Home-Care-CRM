import { EmptyState } from "@/components/shell/empty-state";

export default function VisitsStub() {
  return (
    <EmptyState
      title="Visits"
      description="Live status board, EVV exceptions, and clock-in/out review land in Phases 3–4."
    />
  );
}
