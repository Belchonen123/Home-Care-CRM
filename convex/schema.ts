import { defineSchema } from "convex/server";

/**
 * Domain schema is intentionally empty in this commit.
 *
 * Filled in Prompt 2 per HOMECARE_CRM_SPEC.md §4 (Domain Model).
 */
export default defineSchema({});
